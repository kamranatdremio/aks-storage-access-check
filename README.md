# AKS → Azure Storage Access Test (Sample)

This sample demonstrates how to test Azure Storage access from an AKS cluster.
Follow the scripts to set environment variables, create secrets, deploy a test pod, and verify access.

FIRST RUN the files in the SCRIPTS folder to set the env variables.
